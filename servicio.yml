apiVersion: v1
kind: Service
metadata:
  name: credicoopdeployservice
spec:
  selector: 
    nombre: CredicoopWeb  # Corregido aquí
    tier: backend         # Añadido aquí
  ports:
    - protocol: 'TCP'
      port: 5000
      targetPort: 5000
  type: LoadBalancer
